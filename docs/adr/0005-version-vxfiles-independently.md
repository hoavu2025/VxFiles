# Version VxFiles independently

Begin VxFiles at version 0.1.0 and identify its upstream base separately as Files 4.2. Independent versioning prevents customized builds from being mistaken for official Files releases and lets VxFiles evolve on its own release cadence.
