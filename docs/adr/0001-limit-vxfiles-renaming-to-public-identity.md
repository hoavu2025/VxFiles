# Limit VxFiles renaming to the public identity

Use VxFiles for user-visible naming, distribution names, executable identity, and protocols, while retaining inherited `Files.*` namespaces, project names, and solution structure. Milestone 1 also retains the current Files icon assets. This keeps the application distinct by name without creating pervasive rename conflicts whenever changes are merged from Files Community; a custom icon can be introduced later.
