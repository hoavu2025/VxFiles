# Keep the Portable Build free of automatic integration

The Portable Build will not automatically register file associations, protocols, startup tasks, background updates, or registry entries. Package-dependent features will be unavailable or hidden, preserving a predictable no-install and no-admin launch experience; optional per-user integration can be designed later as an explicit action.
